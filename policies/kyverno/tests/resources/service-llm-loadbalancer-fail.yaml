apiVersion: v1
kind: Service
metadata:
  name: llm-public-svc
  labels:
    zonaas/workload-type: llm
spec:
  type: LoadBalancer
  selector:
    app: llm
  ports:
    - port: 8080
      targetPort: 8080
