apiVersion: v1
kind: Pod
metadata:
  name: pod-pinned-pass
spec:
  containers:
    - name: app
      image: nginx:1.27.0
      securityContext:
        allowPrivilegeEscalation: false
        readOnlyRootFilesystem: true
