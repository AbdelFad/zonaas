apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${NAME}
  description: ML project scaffolded by ML-LZ
  labels:
    mlz/cost-center: ${COSTCENTER}
    mlz/gpu-profile: ${GPUPROFILE}
    mlz/sla-tier: ${SLATIER}
    mlz/compliance-level: ${COMPLIANCELEVEL}
spec:
  type: service
  owner: ${OWNER}
  lifecycle: production
